# Sanity CMS Configuration
# Get these values from https://www.sanity.io/manage

# Your Sanity project ID (required)
PUBLIC_SANITY_PROJECT_ID=your-project-id

# Dataset name (usually "production")
PUBLIC_SANITY_DATASET=production

# API token for preview/draft content (optional, for preview mode)
SANITY_API_TOKEN=

# Write token for migration script (only needed for initial setup)
SANITY_WRITE_TOKEN=

# Google Tag Manager (optional)
PUBLIC_GTM_ID=GTM-XXXXXXX
